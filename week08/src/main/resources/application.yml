spring:
  application:
    name: week08
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3s
      connect-timeout: 3s
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 1
          max-wait: 2s
          time-between-eviction-runs: 30s
        shutdown-timeout: 100ms

week08:
  redis-demo:
    enabled: true
    key-prefix: week08
    ttl: 30s
    wait-after-write: 35s
